# User Profile Page — Integration Plan

**Версия**: 1.1
**Дата**: November 18, 2025
**Owner**: Frontend + Platform teams

---

## 1. Обзор

Новая страница профиля должна объединить карточку пользователя, редактирование данных, отображение квот/лимитов и подготовку к покупке дополнительных запусков. Платёжная интеграция ещё не выбрана, поэтому в рамках релиза реализуем инфраструктуру и UX с пометкой "coming soon".

---

## 2. Фазы внедрения

| Фаза | Цель | Ключевые задачи | Выходной артефакт | Статус |
| --- | --- | --- | --- | --- |
| **0. Discovery (0.5 спринта)** | Согласовать поля профиля, сценарии квот | - Утвердить схему `ProfileResponse`<br>- Решить, где хранить лимиты (`available_launches` vs `user_quotas`) | Обновлённые API-спеки в Notion/Stoplight | ✅ завершено |
| **1. Backend Foundation (1 спринт)** | Подготовить API и данные | - Alembic `008_add_profile_optional_fields`<br>- Обновить `profile_service` (`GET/PATCH /api/profile/me`)<br>- Создать `GET /api/billing/quotas/preview` (stub)<br>- Unit-тесты (`test_profile_api.py`) | API работает локально | ✅ (activity REST остаётся) |
| **2. Frontend Page (1 спринт)** | Реализовать страницу `/profile` | - Роут+навигация<br>- Компоненты: Overview, Form, Quota, Activity
- Хуки `useProfile`, `useQuotaPlans` | PR с UI, тёмная тема, адаптивность | ✅ за merge 2025-11-18 |
| **3. Payments Placeholder (0.5 спринта)** | Подготовить UX под покупку | - Модалка тарифов + предупреждение<br>- Feature flag `REACT_APP_ENABLE_PROFILE_PAYMENTS_UI`<br>- CTA → mailto | Пользователь видит планы и способ связи | ✅ (backend-флаг TBD) |
| **4. Observability & Launch (0.5 спринта)** | Проверить и раскатить | - Логирование/метрики (`profile_view`, `profile_edit`)<br>- QA regression, accessibility audit<br>- Обновить README/docs | Фича включена в staging/prod | 🚧 (аналитика/активность) |

---

## 3. Детализация задач

### 3.1 Backend
- [x] **Модель данных**: alembic `008_add_profile_optional_fields` добавляет `company/timezone/avatar_url` в `profile.user`.
- [ ] **Квоты**: отдельная таблица `user_quotas` отложена. Пока используем `available_launches` и рассчитываем snapshot в `ProfileService`.
- [x] **Сервисы**: `ProfileService` поддерживает CRUD, кэш и PATCH поля. `QuotaService` запланирован (обёртка над job-сервисом).
- [x] **API**:
   - `GET /api/profile/me` реализован и покрыт тестами.
   - `PATCH /api/profile/me` реализован, проверяет разрешённые поля.
   - `GET /api/billing/quotas/preview` возвращает статические планы (coming soon).
   - [ ] `GET /api/profile/activity` — требует аггрегатора событий (см. раздел 3.4).
- [x] **Auth**: все ручки используют `check_auth` и cookie JWT.
- [ ] **Логи**: добавить `logger.info` в PATCH (частично есть) + аудит изменений квот.

### 3.2 Frontend
- [x] **Навигация**: пункт "Профиль" добавлен в `Header`, страница защищена `ProtectedLayout`.
- [x] **API слой**: `API/profile.js`, `useProfile`, `useQuotaPlans`. Хук для `getRecentActivity` появится вместе с backend API.
- [x] **Состояния**: загрузка/ошибка/empty-state/low-quota реализованы в `ProfilePage`.
- [x] **Компоненты**: OverviewCard, EditDrawer, QuotaUsageCard, PurchaseQuotaModal, ActivityTimeline (empty state).
- [x] **Статика**: баннер с `payments@mlservice.ai` включён.
- [ ] **Аналитика**: события не подключены; нужно определить источник (frontend tracker или backend логирование).

### 3.3 Infrastructure / DevOps
- [ ] Env vars: добавить в конфиг backend (`service/settings.py`) параметры `PROFILE_DEFAULT_QUOTA`, `PROFILE_ACTIVITY_WINDOW_DAYS`, `PROFILE_PAYMENTS_CTA_EMAIL`, `ENABLE_PROFILE_PAYMENTS_UI` + пробросить в Helm/docker-compose.
- [x] Frontend flag: `REACT_APP_ENABLE_PROFILE_PAYMENTS_UI` используется для скрытия CTA.
- [ ] Monitoring: панели Prometheus/Grafana + алерты по 4xx/5xx.
- [ ] Alerts: уведомления на `/api/profile/*`, `/api/billing/quotas/preview`.
### 3.4 Backend → Activity integration (новая)
1. **Repository**: собрать события из `profile.user_launch`, `profile.training_run`, `profile.dataset` (LIMIT 20, сортировка по `created_at`).
2. **Model**: `ProfileActivityEvent` (id, type, title, description, timestamp, metadata).
3. **Service**: `ProfileActivityService` нормализует сущности, добавляет человекочитаемые описания.
4. **API**: `GET /api/profile/activity?limit=10` возвращает массив событий, кэшируется в Redis 30 секунд.
5. **Frontend**: добавить `useProfileActivity` хук; текущая `ActivityTimeline` начнёт отображать реальные данные.
6. **Docs/tests**: описать контракт в README/API, покрыть unit-тестами репозитория и e2e тестом в frontend.

---

## 4. Зависимости и блокеры

| Тип | Описание | План действия |
| --- | --- | --- |
| Технический | Нет готовой биллинг-системы | Сделать UX-заглушку + контакт, задокументировать API для будущей интеграции. |
| Данные | Отсутствуют timezone/ company в базе | Миграция + nullable поля, автозаполнение пустыми значениями. |
| Безопасность | Изменение профиля должно логироваться и требовать re-auth, если email менять (пока email readonly) | Ограничить список редактируемых полей, добавить аудит. |
| UI | Не согласованы макеты | Использовать существующую дизайн-систему (Card, Stack, Progress) и подготовить Figma wireframe. |

---

## 5. Чеклист запуска

1. **Backend**
   - [x] Миграции (dev) — `alembic upgrade head` с 008.
   - [x] Тесты `pytest backend/tests/test_profile_api.py` зелёные.
   - [x] Ручки `GET/PATCH /api/profile/me` и `GET /api/billing/quotas/preview` работают.
   - [ ] `GET /api/profile/activity` реализован и протестирован.
2. **Frontend**
   - [x] `npm run lint && npm run test` (новые компоненты) без ошибок.
   - [x] Страница `/profile` отображается в светлой/тёмной темах.
   - [x] CTA отображает предупреждение об оплате.
   - [ ] ActivityTimeline получает данные с backend.
3. **Docs & Ops**
   - [ ] Обновлены `README`, `docs/profile_page_*`.
   - [ ] Саппорт знает, куда приходят запросы на покупку (почта/Slack канал).
   - [ ] Мониторинг и алерты прогнаны на staging.
4. **Rollout**
   - [ ] Включить feature flag в staging → smoke test.
   - [ ] Включить в production для 10% пользователей (если фичи-флаги).
   - [ ] Собрать фидбек.
   - [ ] Добавить в release-notes ссылку на `/profile` и описать ограничения (activity beta, платежи soon).

---

## 6. Коммуникация

- **Product**: подтвердить тексты и сценарии оплаты-заглушки.
- **Support**: подготовить макрос/скрипт ответов на запросы "увеличьте квоту".
- **Legal**: предупредить, что платёжные опции в разработке, чтобы не нарушить публичные обещания.
- **Marketing**: анонс в релиз-нотах и в интерфейсе (tooltip на CTA).

---

## 7. Roadmap после MVP

1. Выбор платёжного провайдера (Stripe/Tinkoff/CloudPayments) и финализация API.
2. Поддержка купонов/промокодов.
3. Автоматические напоминания по email об исчерпании квоты.
4. Возможность просматривать историю платежей внутри профиля.
5. Настройки уведомлений (webhook, Slack, email).

---

**Документ подготовлен**: GitHub Copilot
**Утверждение**: ожидает review Product & Engineering leads
