# User Profile Page — Implementation Summary

**Дата**: November 18, 2025
**Статус**: � Backend+Frontend MVP собран (≈80% build / 20% polish). Активность и телеметрия в работе.
**Приоритет**: 🎯 Высокий (требуется для улучшения UX и подготовки к платным квотам)

---

## 🎯 Цели и ожидаемый результат

1. **Прозрачность для пользователя** — единая страница, где отображается профиль, контактные данные, подписка и технические лимиты (кол-во запусков обучения, дата следующего обновления квоты и т.д.).
2. **Самообслуживание** — пользователь должен редактировать основные данные (имя, компания, страна, timezone) без обращения к поддержке.
3. **Монетизация** — подготовить интерфейс и API-контракты под покупку дополнительных запросов. На бэкенде оплат пока нет, поэтому выводим **баннер-предупреждение «Платёжная интеграция скоро будет доступна»** и готовим все технические точки расширения.
4. **Диагностика проблем** — на странице должны быть краткие логи активности (последний запуск обучения, последнее списание квоты) и блок «какому тарифу соответствует пользователь».

---

## 📦 Объём работ

### Must Have (MVP)
- ✅ Чтение профиля через `GET /api/profile/me` (FastAPI, расширена схема `UserProfileLogic`, кэш + quota snapshot).
- ✅ UI-блок "Мой профиль" + Drawer для редактирования (имя, компания, timezone, avatar URL, телефон).
- ✅ Блок "Квоты и использование" с CTA "Купить" → модалка тарифов (coming soon) + фича-флаг `REACT_APP_ENABLE_PROFILE_PAYMENTS_UI`.
- 🚧 Карточка "История активности": UI-заглушка готова, требуется backend-ручка `GET /api/profile/activity` и аггрегация событий (см. раздел "Интеграция с активностью").
- ✅ Предупреждение при низкой квоте (`available_launches` <= 30% или <=3) + баннер.

### Should Have
- ✅ Фича-флаг UI (`REACT_APP_ENABLE_PROFILE_PAYMENTS_UI`). Backend-флаг `ENABLE_PROFILE_PAYMENTS_UI` добавим вместе с конфигом.
- ✅ FAQ/Support блок с `payments@mlservice.ai` и mailto CTA.
- ✅ Плейсхолдер Purchase modal (статические планы, статус `coming_soon`).
- 🚧 Трекинг событий `profile_view/profile_edit/profile_quota_cta_click` (нужно подключить monitoring SDK, пока не реализовано).

### Could Have (после релиза)
- Загрузка аватара (S3/MinIO).
- Связка нескольких организаций к одному профилю.
- Настройка уведомлений о скором исчерпании квоты.
- 💡 Activity feed на базе event store (webhooks/Redis Streams) и уведомления.

---

## 🧱 Архитектура решения

| Слой | Изменения |
| --- | --- |
| **Backend** |
- ✅ Расширена `UserProfileLogic` + alembic `008_add_profile_optional_fields`.
- ✅ `ProfileService` возвращает snapshot квот, поддерживает PATCH, кэш.
- ✅ `GET/PATCH /api/profile/me`, `GET /api/billing/quotas/preview`.
- 🚧 `GET /api/profile/activity` — спроектирован (см. ниже), но не реализован.
- ❌ Отдельного `QuotaService` пока нет (используем `available_launches`). Решено отложить до рефакторинга job-сервиса.
| **Frontend** |
- ✅ Роут `/profile` + пункт меню "Профиль" для авторизованных.
- ✅ Состояния загрузки/ошибок/пустых данных реализованы в `ProfilePage`.
- ✅ Компоненты (Overview/Drawer/Quota/Modal/Activity) и хуки `useProfile`, `useQuotaPlans`.
- 🚧 ActivityTimeline показывает заглушку до подключения backend API.
| **Infrastructure** |
- ✅ Клиентская переменная `REACT_APP_ENABLE_PROFILE_PAYMENTS_UI`.
- 🚧 Backend env: `PROFILE_DEFAULT_QUOTA`, `PROFILE_ACTIVITY_WINDOW_DAYS`, `PROFILE_PAYMENTS_CTA_EMAIL`, `ENABLE_PROFILE_PAYMENTS_UI` — задокументированы, нужно внести в `service/settings.py`.
- 🚧 Аналитика/мониторинг подключим на Observability фазе.

---

## 🔌 API и контракты

### 1. `GET /api/profile/me`
```jsonc
{
  "id": "uuid",
  "email": "user@example.com",
  "first_name": "",
  "company": "Acme AI",
  "timezone": "Europe/Moscow",
  "created_at": "2025-09-01T10:00:00Z",
  "role": "user",
  "quota": {
    "limit": 50,
    "used": 32,
    "available": 18,
    "resets_at": "2025-12-01T00:00:00Z"
  }
}
```

### 2. `PATCH /api/profile/me`
```jsonc
// Request
{
  "first_name": "Иван",
  "company": "MLservice",
  "timezone": "Europe/Moscow"
}
```
Ответ — обновлённый профиль.

### 3. `GET /api/billing/quotas/preview` (stub)
- Возвращает массив пакетов: `[{"id":"starter","tokens":10,"price":0,"status":"coming_soon"}, ...]`
- UI покажет список и предупреждение "Оплата будет доступна позже. Свяжитесь с нами для ручного расширения".

---

## 🧭 Пользовательские сценарии

1. **Просмотр**: пользователь открывает `/profile`, видит карточку с данными, квоту (прогресс-бар), список последних запусков.
2. **Редактирование**: нажимает "Изменить", меняет поля, сохраняет → toast + optimistic update.
3. **Квоты**: при <20% оставшихся запросов появляется предупреждение и CTA.  При клике открывается модалка с тарифами и кнопкой "Связаться с нами" (mailto).
4. **Активность**: после реализации backend-ручки `GET /api/profile/activity` UI покажет 5 событий (job start, dataset upload, quota change). Сейчас отображается empty-state.
5. **Пустой статус**: если нет активности, выводим заглушку.
6. **Ошибка API**: используем `showErrorToast` + inline alert.

---

## 🚧 Риски и ограничения

| Риск | Митигирующая мера |
| --- | --- |
| Нет готовой платежной интеграции | Чётко помечаем, что покупка пока недоступна. Кнопка открывает модалку с текстом и ссылкой в поддержку. API возвращает `status: "coming_soon"`. |
| Расхождение лимитов между job_service и profile_service | Создаём единый `QuotaService` (обёртка вокруг существующей логики), синхронизируем расчёт `available_launches`. |
| Неконсистентные данные профиля | Ограничить редактируемые поля, применять server-side валидацию и логгировать изменения. |
| UX перегружен | Разделить страницу на карточки, использовать существующий дизайн-систему (Card, GlowingCard, Progress). |

---

## ✅ Критерии приёмки

- [x] `/profile` доступен из основного меню и защищён роутером.
- [x] Профиль корректно загружается/обновляется, ошибки отображаются человекочитаемо.
- [x] Квота визуализирована и совпадает с данными backend (`available_launches`).
- [x] CTA "Купить" отображает планы и предупреждение об отсутствии онлайн-оплаты.
- [ ] Отправка аналитических событий (`profile:view`, `profile:update`, `profile:cta_buy_quota`).
- [ ] Activity feed получает данные из backend (`GET /api/profile/activity`).
- [x] Документация (integration plan + migration guide) лежит в `docs/`.

---

## 🚀 Следующие шаги

1. **Backend activity API** — агрегировать события (`user_launch`, `training_run`, `dataset`) и добавить `GET /api/profile/activity` + unit-тесты.
2. **Quota service** — определить источник лимитов (возможно, `user_quotas` таблица) и синхронизировать с `available_launches`.
3. **Frontend analytics** — отправлять `trackEvent` на mount/save/CTA.
4. **Payments placeholder** — доработать конфиг-флаги backend и добавить обновляемые планы.
5. **QA** — чеклисты: редактирование, лимит 0/>0, ошибки API, пустая активность, скрытый CTA.

## 🔄 Интеграция с активностью backend

1. **Источник данных**: используем таблицы `profile.user_launch`, `profile.training_run`, `profile.dataset` (и в будущем `profile.job_events`).
2. **Сервис**: добавить `ActivityRepository` с запросом последних N событий по `user_id`, нормализовать их к общей модели `ProfileActivityEvent`.
3. **API**: реализовать `GET /api/profile/activity?limit=10` (в `profile_router`), требует авторизации через `check_auth`.
4. **Кэширование**: допускаем кеш Redis на 30 секунд, чтобы не грузить базу при обновлениях страницы.
5. **Frontend**: `ActivityTimeline` получает реальные данные, показывает тип события, название и timestamp, fallback на EmptyState, если массив пуст.
6. **MVP метрики**: добавить лог `profile_activity_fetched` + Prometheus counter для количества событий.

---

**Owner**: Frontend/Platform team
**Документ подготовлен**: GitHub Copilot, November 18, 2025
