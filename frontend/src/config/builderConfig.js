// Builder UX/config
export const connectionLimitPerHandle = 1; // default per-handle connection limit

// Highlight rules: currently only source -> targets highlighting
export const highlightMode = {
  sourceToTargets: true,
  targetToSources: false, // toggle if implementing reverse highlighting
};
